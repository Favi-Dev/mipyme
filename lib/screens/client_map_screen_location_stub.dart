/// Stub implementation — should never be used at runtime.
/// Dart's conditional imports will pick the web or native version instead.
Future<Map<String, double>?> getCurrentLocation() async {
  return null;
}
